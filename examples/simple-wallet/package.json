{
  "name": "@sails/example-simple-wallet",
  "version": "0.1.0",
  "private": true,
  "description": "Dogfooding example: the smallest real wallet integration of @sails/sdk — no mocks, runs against a real local Sails node. Answers one question: can a wallet integrate this SDK in under 15 minutes?",
  "scripts": {
    "start": "ts-node --transpile-only src/index.ts"
  },
  "dependencies": {
    "@sails/sdk": "^0.1.0"
  }
}
