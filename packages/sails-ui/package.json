{
  "name": "@sails/ui",
  "version": "0.0.1",
  "private": true,
  "description": "Sails P2P Trading SDK reference UI — a functional prototype (mocked data, plain Tailwind, no final visual identity yet) proving the trading loop's navigation and state flow before a themed pass. Not published, not the SDK itself — see docs/TODO.md section 11 and this package's own README.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2",
    "sonner": "^1.5.0",
    "recharts": "^2.12.7"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.4.2",
    "vite": "^8.1.5"
  }
}
