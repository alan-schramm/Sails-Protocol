// Skeleton phase (docs/TODO.md section 11): plain Tailwind defaults, no
// Satsails brand palette yet — the "roupagem" (visual identity: WDK/
// Binance-inspired dark+orange design system) is deliberately a separate
// later pass, not this one. Keeping this file undecorated is intentional,
// not an oversight.
/** @type {import('tailwindcss').Config} */
export default {
  content: ['./index.html', './src/**/*.{ts,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
}
