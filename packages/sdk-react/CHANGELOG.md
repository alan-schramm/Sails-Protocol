# Changelog

All notable changes to `@satsails/sdk-react` are documented here.

## [0.2.0] - Unreleased

### Changed
- Prepare the package artifact identity for the controlled `0.2.0` release line.
- Require `@satsails/p2p-trading-sdk` `^0.2.0` for the published dependency surface.
- Remove the unused `hardhat` runtime dependency; no repository usage was found to justify it as public runtime surface.

This is a source-state preparation record only. Publication, registry verification, tagging, and GitHub Release completion remain separate controlled release steps.
