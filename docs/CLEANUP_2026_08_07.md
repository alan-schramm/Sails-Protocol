# Repository Cleanup — 2026-08-07

> **Scope**: Removed transient files and one stray test. No production
> code, configuration, or documentation was changed.

## Summary

| Category | Count | Impact |
| --- | --- | --- |
| Test output files removed | 8 | All were stale `npm test` / `tsc` redirect outputs from previous refactoring work |
| Stray test files removed | 1 | `useSailsProof.test.tsx` was duplicated at repo root; the real one lives at `packages/sdk-react/tests/useSailsProof.test.tsx` |
| Redis dump removed | 1 | `dump.rdb` was a local Redis snapshot, already in `.gitignore` |

**Total: 10 files removed.** Zero production code, zero tests, zero docs changed.

## Files Removed

```
all-tests-out.txt        # npm test redirect output
all-tests.txt            # npm test redirect output
clean.txt                # npm test redirect output (post-refactor verification)
final-tests.txt          # npm test redirect output
final.txt                # npm test redirect output
sdk-log.txt              # jest debug output
sdk-test-out.txt         # sdk jest debug output
tsc-out.txt              # tsc --noEmit redirect output
dump.rdb                 # local Redis dump
useSailsProof.test.tsx   # duplicate at repo root (real copy in packages/sdk-react/tests/)
```

## What Was NOT Removed

Investigated and explicitly kept:

- **`scripts/sdk-failure-injection.ts`** and **`scripts/sdk-pressure-test.ts`**: Manual testing utilities (no `package.json` script, no doc reference). Kept as a "delete me next time someone explicitly asks" candidate.
- **`.local-pgdata.log`** / **`.local-redis.log`** / **`.local-redis.pid`**: Runtime output from `scripts/local-postgres.js` and `scripts/local-redis.js`. Already in `.gitignore`. Kept untouched.
- **`loadtest/` and `load-tests/`**: Two separate load-testing suites (Artillery + k6). Both have README explaining why both exist.
- **`src/types.d.ts`**: Ambient module declarations for `hyperdht` / `hyperswarm` / `b4a`. Required by `tsc --noEmit` to silence implicit-any.
- **`src/modules/open-agents/social-engineering-agent.ts`**: Loaded lazily via `require()` inside an event handler conditional, not via static import. Static-import analyzers won''t see it.

## Validation

| Check | Result |
| --- | --- |
| `npx tsc --noEmit --noUnusedLocals --noUnusedParameters` | 0 errors (no dead imports in production code) |
| `npx tsc --noEmit` | 0 errors |
| `npm test` | 57 suites, 689 tests passing (51s) |

## No Documentation Drift

- No `docs/` files touched
- No `CHANGELOG.md` entry added (the cleanup itself doesn''t change behavior; nothing to announce)
- No `.gitignore` change needed (removed files were already covered or are transient output not worth tracking)
